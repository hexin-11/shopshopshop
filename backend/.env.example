PORT=8787

# 当前默认使用 json，本地比赛演示无需数据库。
# 之后接数据库时改成 database，并在 storage/databaseAssetStore.js 实现具体逻辑。
ASSET_STORE=json

# 当前默认使用 json，本地比赛演示无需数据库。
# 之后接数据库时改成 database，并在 storage/databaseAppStore.js 实现具体逻辑。
APP_STORE=json

# 预留数据库连接配置，当前不会读取真实数据库。
DATABASE_URL=
